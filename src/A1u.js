
const A1u = ({showlist,id}) => {
  const update = showlist[id];


  return (

     <>
         <h1>UPDATE DATA</h1>


     {/* {
        showlist.map((list,i)=>{
            return(
                <>
                    <h2>{list.name}</h2>
          <h2>{list.lname}</h2>
          <h2>{list.skills.join(", ")}</h2>  
                </>
            );
        })
     } */}
     </>
  );
};

export default A1u;
